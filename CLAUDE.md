# プロジェクト運用ルール

## 役割分担
- 計画・判断: Fable(オーケストレーター)。実装コード: Sonnetサブエージェント
- 事業計画と実装指示は `PLAN.md`。マイルストーン(M1〜M6)単位で実装を進める

## 利用上限(レートリミット)からの自動復帰 — 必須
**ユーザーは「制限が解除された後、指示なしで自動的に作業が継続・進行すること」を明示的に要求している。** Fable(オーケストレーター)は以下を徹底すること。

### A. サブエージェントが上限で停止した場合
サブエージェントが利用上限で停止した場合(例: "You've hit your session limit · resets 3pm (UTC)")、**ユーザーの指示を待たずに**次を実行すること:
1. その時点までの差分・コミット状況・テスト結果を確認し、どこまで完了したかを特定する
2. **同じマイルストーンの残作業を引き継ぐ新しいサブエージェントを直ちに起動する**(M3で実証済みの最有効パターン)。引き継ぎ用プロンプトには、完了済み項目・残項目・検証基準(`pnpm -r test`/`pnpm -r typecheck`)を明記する
3. 並行して、念のためバックグラウンドタイマー(Bash `run_in_background`)も「リセット時刻+3分」まで起動しておく(例: `while [ "$(date -u +%s)" -lt <リセットepoch+180> ]; do sleep 60; done`)。新サブエージェントも上限に達した場合のフォールバックとして機能する
4. 新サブエージェントが正常に動き始めたら、その完了を待つ。ユーザーへの報告はマイルストーン完了時(後述B)にまとめて行えばよく、その都度の確認は不要

### B. マイルストーン完了時 — 次マイルストーンへの自動移行
1マイルストーンの実装・テスト・コミット・pushが完了したら、**ユーザーの指示を待たずに直ちに次のマイルストーン(M5→M6など)のサブエージェントを起動する**。全マイルストーン(M1〜M6)が完了するまでこれを繰り返す。各マイルストーン完了時に簡潔な進捗報告をユーザーに送ってよいが、報告は「実行を止めて指示を待つ」ことを意味しない — 報告後も即座に次のマイルストーンに着手する。

### C. メインセッション自体が上限で停止した場合
セッション(Fable自身)が利用上限で停止した場合も、メッセージにリセット時刻が含まれていればバックグラウンドタイマーを起動して待機し、満了後に`git log`とPLAN.mdから中断地点を特定して自動再開する。

注意: セッション自体が終了(コンテナ回収)された場合はこの方法では復帰できない。その場合、新しいセッションで「続きを実装して」と言われたら `PLAN.md` §4.6 と `git log` から未完了マイルストーンを特定して再開すること。

## 実装の検証基準(全マイルストーン共通)
- `pnpm -r test` 全パス / `pnpm -r typecheck` 全パス
- この環境には `ANTHROPIC_API_KEY`・実機・Neon接続がない。実API呼び出しは行わず、PGlite統合テストとローカルcurl検証まで
- コミットは意味のある単位で分割し、ブランチ `claude/business-idea-planning-mdjjli` にプッシュ
- コード内のClaudeモデルIDは `claude-haiku-4-5` / `claude-sonnet-4-6` を正確に使用(日付サフィックス禁止)
